server:
  port: 8899
  ssl:
    key-store: classpath:3541481_www.uniskare.xyz.pfx
    key-store-password: WkfrAKOh
    key-store-type: PKCS12
    enabled: true

eureka:
  instance:
    ip-address: 47.102.158.185
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://49.235.255.145:8991/eureka/





spring:
  application:
    name: api-gateway-service
  cloud:
    gateway:
      routes:
      - id: user-module
        uri: lb:http://user-provider
        predicates:
        - Path=/user/**

      - id: order-module
        uri: lb:http://order-provider
        predicates:
        - Path=/orders/**

      - id: skill-module
        uri: lb:http://skill-provider
        predicates:
        - Path=/skill/**

      - id: comm-module
        uri: lb:http://communication-provider
        predicates:
        - Path=/comm/**
      discovery:
        locator:
          enabled: true




